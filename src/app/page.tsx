import LanguageData from "@/components/LanguageData";

const Home = () => {
  return (
    <div className="flex-wrap h-screen w-screen items-center justify-center overflow-x-hidden">
      <LanguageData languageCode="1" />
    </div>
  );
};

export default Home;
